Signature error -> because new venv every time
emulator -> clean install signature the same

**keystore**
To get the architecture your device is using use **adb -s [phone_ip]:[port] shell getprop ro.product.cpu.abi**

