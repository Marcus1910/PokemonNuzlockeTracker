class Area():
    def __init__(self, name):
        self.name = name
        self.line = int
        #list of lists, [grass Normal, grass Double, shaking grass, surf normal, surf dark, grotto]
        #search for list[x][0] for name
        self.springEncounters = list
        self.summerEncounters = list
        self.winterEncounters = list
        self.autumnEncounters = list
        self.allEncounters = list
    def __str__(self):

        return f"name: {self.name}"
